// IMPORTS
import { BookingsTable } from "./components/BookingsTable";

const Bookings = () => {
  // CONTENT
  const content = (
    <section className="bookings">
      <BookingsTable />
    </section>
  );

  return content;
};

export default Bookings;
